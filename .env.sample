# enter your own info

EXPRESS_PORT=3000
SESSION_SECRET=
# used to sign JWTs for the /api routes; any long random string
JWT_SECRET=
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
# init-db.sql creates a database called web_db
DB_DATABASE=web_db
